{
  "name": "data",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pm2 start ecosystem.config.json --watch && pm2 logs --timestamp --merge-logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@pythnetwork/pyth-evm-js": "^1.1.0",
    "alchemy-sdk": "^2.2.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2"
  }
}
